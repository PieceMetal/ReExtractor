# ReExtractor v1.3.5

本版本修复中文安装路径下的在线更新失败问题，并补全升级失败诊断。

## 修复

- 更新脚本改为带 BOM 的 UTF-8，Windows PowerShell 5.1 能正确读取中文路径，不再把目标 EXE 路径解码成乱码。
- 更新脚本失败时将错误写入 `ReExtractor-tools/logs/update.log`，方便直接定位问题。

## 兼容说明

- v1.3.4 及更早版本若安装在包含中文字符的目录中，旧脚本无法修复自身；请手动解压运行一次 v1.3.5。
- v1.3.5 之后，正常的在线更新可在中文目录中使用。
