# v1.3.21

- 在设置中选到 `blender-launcher.exe` 时，自动改用同目录的 `blender.exe`，不运行启动器。
- 已保存的启动器路径在读取时自动修正，转换入口也执行相同检查。
- 同目录缺少 `blender.exe` 时明确提示重新选择主程序，避免启动器退出后仅得到“未收到脚本完成标记”。

验证：路径归一化、直接选择主程序、缺少主程序时在启动前拒绝、旧配置迁移检查通过。此修复覆盖启动器路径误选，不代表所有 FBX 转换失败均已解决；其他失败仍可通过 `ReExtractor-tools/logs/fbx-*.log` 定位。
