# ReExtractor v1.2.4

本版本修复动画切换后的自动播放问题。

## 主要更新

- 在动画下拉框中选择动作后，动画会自动从第 0 帧开始播放。
- 修复程序重置时间轴时被误判为用户拖动，进而立即暂停播放的问题。
- 用户手动拖动时间轴时仍会自动暂停，逐帧预览行为保持不变。

## 下载与环境

- 发布包：`ReExtractor-v1.2.4-win-x64.zip`
- 支持 Windows 10 / 11 x64。
- 压缩包内仅包含 `ReExtractor.Gui.exe`。
- FBX 导出需要单独安装 Blender。
