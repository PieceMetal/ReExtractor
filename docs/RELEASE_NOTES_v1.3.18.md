# v1.3.18

- 修复多分件角色进入骨架兼容导出时，重复骨骼名（例如静御前的 R_Eye）导致 FBX 导出中断的问题。保留主骨架的独立索引，为重复名称生成唯一导出名称。
- 修复界面仍显示旧版本号的问题：窗口标题和顶部文字统一读取程序实际版本。
- 在线列表增加网页目录回退：GitHub API 限流或不可用时，校验目录完整性并使用固定提交版本下载列表。

验证：静御前站立四分件实际预览导出生成合并 FBX、66 张 PNG，其中 16 张 NRRO；已有合并回归检查 18 项通过。在线列表实测获取 93 项并成功下载鬼武者列表。

贴图位置：FBX 配套的 `.fbm` 目录主要保存连接到材质的基础色贴图；法线及辅助贴图保存在同级 `textures` 的资源路径子目录中，可搜索 `NRRO`。它们不会自动连接到 FBX 材质。

已知限制：此次静御前导出仍有两张三维特效纹理不支持 Depth > 1，失败原因会写入导出日志；不影响上述 FBX 和 66 张 PNG 的生成。
